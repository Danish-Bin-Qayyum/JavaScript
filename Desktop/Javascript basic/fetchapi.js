/*const fetchnumberinfo = async ()=> {
    const response = await fetch('link')
    if (response.ok) {
        throw new error('yser data not found')
        
    }
    const userdata = await response.json();
    console.log(userdata);
}

fetchnumberinfo();*/